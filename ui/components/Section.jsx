import styled from 'styled-components';

const Section = styled.section`
  margin: 0.5em;
  padding: 0.5em;
  border-radius: 0.2em;
  background: #eee;
  &:hover {
    background: #ddd;
    cursor: pointer;
  }
`;

export default Section;
